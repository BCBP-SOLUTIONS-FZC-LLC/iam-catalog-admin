#!/usr/bin/env bash
# Local commit gates: formatting and lint.
# Ported from iam-user-profile's .githooks/pre-commit — dropped the Swagger
# staleness check (this service has no Swagger setup).
set -euo pipefail

echo "[pre-commit] running tidy..."
make tidy

echo "[pre-commit] running fmt-check..."
make fmt-check

echo "[pre-commit] running lint..."
make lint
